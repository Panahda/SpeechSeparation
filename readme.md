# Packages
```python
conda install numpy
pip install rotary_embedding_torch
pip install soundfile pydub moviepy modelscope
conda install pytorch torchvision torchaudio cudatoolkit=11.8 -c pytorch -c nvidia
```
